using System;
using UnityEngine;

namespace Core3.MonoBehaviors
{
    public class GameManager : BaseManager
    {
        private async void Start()
        {
            // var User = await HttpClient.Get<User>("https://api.luisboyd.co.uk/User?id=100");
            // //var User = await HttpClient.Get<User>("https://localhost:7003/User?id=100"); //Testing
            // Debug.Log(User.Id.ToString());
            // Debug.Log("FoundUser");
        }
    }
}