using RCR.Enums;
using RCR.Patterns;

namespace Gameplay
{
    public class MapSectionController: BaseController<MapSectionModel>
    {
        //public void SetTileValue(TileType type, int x, int y) => SetTileValue((byte)type, x, y);
        //public void SetTileValue(byte type, int x, int y) => Model[x, y] = type;

        //public void SetTileValues(byte[] value) => Model.SectionByteData.Value = value;
    }
}